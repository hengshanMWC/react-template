export const baseUrl = import.meta.env.VITE_APP_HOST

export const timeout = 10000

export const uploadPath = '/api/upload'

export const uploadUrl = baseUrl + uploadPath

export const title = 'React Template'
